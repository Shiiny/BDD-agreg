{% extends 'base.html.twig' %}

{% block title 'Admin' %}

{% block body %}
    <div class="container">
        <div class="col-lg-4">
            <div class="card">
                <div class="card-body">
                    <h2>Editer l'enseignant</h2>
                    {{ form_start(form) }}
                    {{ form_rest(form) }}
                    <button class="btn btn-primary">Editer</button>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>

    </div>

{% endblock %}