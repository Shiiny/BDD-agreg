{% extends 'base.html.twig' %}

{% block title 'Admin' %}

{% block body %}
    <div class="container mt-4">
        {% for label, messages in app.flashes() %}
            {% for message in messages %}
                <div id="flash" class="alert alert-{{ label }}" role="alert">
                    <p class="message">Message flash : {{ message }}</p>
                </div>
            {% endfor %}
        {% endfor %}
        <div class="card">
            <div class="card-body">
                <h1>Gérer les disciplines</h1>
                <div class="text-right mb-3">
                    <a href="{{ path('admin.discipline.new') }}" class="btn btn-secondary">Ajouter</a>
                </div>
                <table class="table table-striped">
                    <thead>
                    <tr>
                        <th>Nom</th>
                        <th>Enseignant</th>
                        <th>Action</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for discipline in disciplines %}
                        <tr>
                            <td>{{ discipline.title }}</td>
                            <td></td>
                            <td>
                                <a href="{{ path('admin.discipline.edit', {id:discipline.id}) }}" class="btn btn-primary">Editer</a>
                                <form method="post" action="{{ path('admin.discipline.delete', {id: discipline.id}) }}" style="display: inline-block" onsubmit="return confirm('Êtes-vous vraiment sûr ?')">
                                    <input type="hidden" name="_method" value="DELETE">
                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ discipline.id) }}">
                                    <button class="btn btn-danger">Supprimer</button>
                                </form>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>

        
    </div>

{% endblock %}